## TASKS

1. Change "gempilot new" to "gempilot create"
2. Change "gempilot add" to "gempilot new". This will maintain consistency with how this pattern normally works, e.g. rails
3. Create new action, "gempilot destroy", which does opposite of "add". Reference the thor repo I have provided for examples on what this could look like. BUT COMMANDKIT IS STILL WHAT WE USE. DO NOT USE THOR FOR ANYTHING ELSE
