<% if @hyphenated -%>
require "<%= @require_path %>"
<% else -%>
require "zeitwerk"

module <%= @module_name %>
  LOADER = Zeitwerk::Loader.for_gem
  LOADER.setup

  class Error < StandardError; end
end
<% end -%>
